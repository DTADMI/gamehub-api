package ca.dtadmi.gamehubapi.security.oauth;

// Placeholder: OAuth2 login flow is not used (frontend handles OAuth via Firebase).
// This no-op handler remains only to keep references stable if any configuration still wires it.
public class OAuth2AuthenticationSuccessHandler {
    // Intentionally empty
}
